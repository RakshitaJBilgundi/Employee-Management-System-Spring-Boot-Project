/*
 * package com.example.oragnization.dto; import lombok.Data; import
 * com.example.oragnization.entity.Department; import
 * jakarta.validation.constraints.NotNull;
 * 
 * @Data public class EmployeeDTO { private int empId;
 * 
 * @NotNull(message = "Age is required") private int age; // -> generates
 * setAge(int age) private String empName; // -> generates setEmpName(String
 * empName) private String location;
 * 
 * }
 */
